#!/bin/bash
source .env

VER="$VERSION"
REG="$REGISTRY"
BTAG="$TAG"

if [ "$1" != "" ]; then
  VER=$1
fi

docker compose build app-prod && \
docker tag $BTAG $REG/$BTAG:$VER && \
docker push $REG/$TAG:$VER
